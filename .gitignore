# Rust build
/target
**/*.rs.bk

# Secrets and local environment (never commit)
# Scoped to project root so vendor/ test fixtures (*.key, *.pem, *.env) are not excluded.
/.env
/.env.*
/etc/propolis/

# Runtime artifacts and local data
/feed
/data

# Internal design, threat-model, and process docs - kept local only, never committed.
# These describe how the honeypot detects and logs; publishing them hands an attacker
# the blueprint. Removed from tracking after they were found on the public repo.
/internal/
/docs/superpowers/

# OS and editor
.DS_Store
Thumbs.db
*.swp
*~
.idea/
/.vscode/

# Claude Code
.claude/
