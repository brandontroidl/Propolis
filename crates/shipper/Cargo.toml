[package]
name = "shipper"
version = "0.1.0"
edition = "2024"

[dependencies]
log-tailer = { path = "../log-tailer" }
collector-wire = { path = "../collector-wire" }

[dev-dependencies]
tempfile = "3.27.0"
